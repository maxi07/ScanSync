pika==1.3.2
ocrmypdf==16.10.4
# Pin pikepdf < 10: pikepdf 10.0.0 removed Pdf.check(), which ocrmypdf 16.10.4 still
# relies on in check_pdf(). Without this pin, OCR fails with
# "'pikepdf._core.Pdf' object has no attribute 'check'".
pikepdf<10